apiVersion: v1
kind: Service
metadata:
  name: backend-a-service
  namespace: microservices-app
spec:
  type: NodePort
  selector:
    app: backend-a
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080